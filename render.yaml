services:
  - type: web
    name: pixelboard-backend
    env: node
    rootDir: Backend/server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
